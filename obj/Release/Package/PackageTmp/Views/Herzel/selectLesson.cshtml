@model eyemusic45.Models.ViewModels.AddLessonsModel
@{
    Layout = "";
}

<html>
<body>


    <h2>בחר שיעור</h2>
    
    <table>
    @for (int i = 0; i < Model.allDirectory.Length;i++)
    //foreach (string DirNAme in Model.allDirectory)
    {
        <tr>
            <td>
                @using (Html.BeginForm("learnLesson", "Herzel", FormMethod.Post, new { id = "submitForm" }))
                {
                    <input type="submit" value="@Model.allHebrewDir[i]" />
                    <input type="hidden" name="DirName" value="@Model.allDirectory[i]" />
                }
            </td>
        </tr>
    }
</table>
</body>
</html>